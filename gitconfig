[user]
	name = walker-interlnkd
  email = walker@interlnkd.com
[color]
  diff = auto
  status = auto
  branch = auto
  ui = true
[alias]
  lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
[push]
  default = matching
[diff]
  tool = icdiff
[core]
  excludesfile = ~/.gitignore
  editor = vim
  quotepath = false
  filemode = true
  ignorecase = false
[fetch]
  prune = true
[remote "origin"]
  prune = true
  pruneTags = true
[pager]
  diff = diff-so-fancy | less --tabs=4 -RFX
  show = diff-so-fancy | less --tabs=4 -RFX
